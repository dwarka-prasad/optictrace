global:
  scrape_interval: 10s
  evaluation_interval: 10s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  # Host network: the shop's agent binds 127.0.0.1:9095 and stays there.
  - job_name: optictrace-shop
    static_configs:
      - targets: ["127.0.0.1:9095"]
